const User = require( "./userDao.js" );
describe( 'UserDao', () =>
{
    describe('getUsers', () => {
        test('If resolved the function must return Array type of output', async () => {
          
        });
    });
});